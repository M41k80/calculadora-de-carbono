import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  experimental: {
    // The appDir option is no longer needed as it's enabled by default in newer Next.js versions
  },
};

export default nextConfig;
